{
  "name": "mastodon_bot",
  "version": "1.0.0",
  "description": "making a bot which can read write and follow users",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^6.0.0",
    "mastodon-api": "^1.3.0"
  }
}
